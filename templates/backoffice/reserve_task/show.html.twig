{% extends 'base.html.twig' %}

{% block title %}Tâches{% endblock %}

{% block body %}
    <h1>Tâches en réserve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ reserve_task.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ reserve_task.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_reserve_task_index') }}">Retour à la liste des tâches</a>

    <a href="{{ path('app_backoffice_reserve_task_edit', {'id': reserve_task.id}) }}">Modifier</a>

    {{ include('backoffice/reserve_task/_delete_form.html.twig') }}
{% endblock %}