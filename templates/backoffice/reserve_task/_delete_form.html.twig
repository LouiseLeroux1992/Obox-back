<form method="post" action="{{ path('app_backoffice_reserve_task_delete', {'id': reserve_task.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cette tâche ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reserve_task.id) }}">
    <button class="btn" style="background-color: #058240; color: #fee3a2">Supprimer</button>
</form>
