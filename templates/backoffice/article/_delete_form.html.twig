<form method="post" action="{{ path('app_backoffice_article_delete', {'id': article.id}) }}" onsubmit="return confirm('êtes-vous sûr de vouloir supprimer cet article ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.id) }}">
    <button class="btn" style="background-color: #058240; color: #fee3a2">Supprimer</button>
</form>
