<form method="post" action="{{ path('app_backoffice_theme_delete', {'id': theme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme.id) }}">
    <button class="btn" style="background-color: #058240; color: #fee3a2">Supprimer</button>
</form>
